#include<bits/stdc++.h>
using namespace std;

#define ll long long

int main()
{
    ll t;
    cin >> t;
    while(t--){

        ll a;
        string s;
        cin >> a;
        cin >> s;

        char x;

        for(ll i=0; i<a-1; i++){
            if(s[0] >= s[i+1]){
                x = s[i];
                //x.push_back(s[i]);
               // s.erase(s[i]);
                //s.insert(0, x);
                //cout << s << endl;
            }

            string y;




        }

    }

    return 0;
}
