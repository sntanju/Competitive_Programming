#include<iostream>
#include <bits/stdc++.h>
#include<math.h>
using namespace std;

int main()
{
    int a, b, result;
    cin >> a >> b;

    result = pow(a, b);
    cout << result << endl;
    return 0;
}
